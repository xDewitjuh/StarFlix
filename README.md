# Starflix

## Tech description

- frontend: HTML, CSS and JS
- backend: Javascript
- database: Postgresql

## How to run

Simply have docker installed with docker compose and run the following command.

Run: `docker compose up`\
This will start the project and database
_To stop the project, simply hit ctrl-c in the terminal, to clean up run `docker compose down` afterwards._